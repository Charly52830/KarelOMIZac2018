class program {
define backturn()
{
	turnleft();
    turnleft();
}
define turnright()
{
	turnleft();
    turnleft();
    turnleft();
}
define napadas(n)
{
	pickbeeper();
    if(nextToABeeper)
    	{
        pickbeeper();
        turnleft();
        iterate(n)
        	{
            if(notNextToABeeper)
            	{
                putbeeper();
                putbeeper();
                }
            move();
            }
        backturn();
        while(frontIsClear)
        	move();
        turnleft();
        move();
        }
    else
    	{
        turnleft();
        iterate(n)
        	{
            if(notNextToABeeper)
            	putbeeper();
            move();
            }
        backturn();
        while(frontIsClear)
        	move();
        turnleft();
        move();
        napadas(succ(n));
        turnleft();
        iterate(n)
        	{
            if(notNextToABeeper)
            	putbeeper();
            move();
            }
        backturn();
        while(frontIsClear)
        	move();
            turnleft();
        if(frontIsClear)
        	move();
        }
}
    program () {
    	turnright();
        napadas(1);
        turnoff();
    }
}

